<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <group android:checkableBehavior="single">
        <item android:id="@+id/popularity_menu_item"
             android:title="@string/Popularity_Title"
             android:checked="true"
            >
        </item>
        <item android:id="@+id/top_rated_menu_item"
             android:title="@string/Top_Rated_Title"
             >
        </item>
    </group>
    <item android:id="@+id/FavoriteFilms"
         android:title="@string/Favorite_Films_Title"
        android:orderInCategory="1"
        app:showAsAction="always"
        android:name="android:itemTextAppearance"
        >
        @style/myCustomMenuTextAppearance
    </item>

</menu>